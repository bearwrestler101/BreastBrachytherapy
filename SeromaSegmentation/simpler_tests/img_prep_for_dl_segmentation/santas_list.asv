function [] = santas_list(binImg, pred)
    [toss,bigBlob] = max(vertcat(pred));
    
    for i = 1:length(pred)
        if i == bigBlog
            disp("main area skipped");
        end
        binImg = ~imfill(~binImg, [pred{2,i}(2) pred{2,i}(1)]);

    end



end




imshow(predImg{11})
hold on
plot(mean(1,1),  mean(1,2), 'ro', 'MarkerSize', 5)
plot(pred{2,11}(1), pred{2,11}(2),'bo','MarkerSize',5)

figure
imshow(predImg{4})
hold on
plot(mean(1,1),  mean(1,2), 'ro', 'MarkerSize', 5)
plot(pred{2,4}(1), pred{2,4}(2),'bo','MarkerSize',5)

end

